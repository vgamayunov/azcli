# Session State — 2026-04-20

## Project
`/home/victor/omo/azcli` — Rust implementation of `az` CLI. Branch `main`, remote `git@github.com:vgamayunov/azcli.git`.

## HEAD
`1fbd6dc feat: add vm run-command subgroup (invoke, list, show, create, update, delete)` — pushed, working tree clean.

## Recent Commits (this session)
- `1fbd6dc` feat: vm run-command (6 cmds: invoke, list, show, create, update, delete) — dual-mode list/show (per-VM persistent OR per-location built-in), legacy POST invoke, full LRO for create/update/delete
- `79cd5d0` fix(cli): use `-g` short for `--resource-group` (71 occurrences in main.rs) — matches real az
- `d4db3cc` feat: vm nic subgroup (list, show, add, remove, set) with `apply_primary` invariant helper
- `ce9e545` feat: managed disk commands (`disk` group 8 cmds + `vm disk` 2 cmds)
- `daf6ab4` fix(vm): use `client.subscription_id()` in open-port and auto-shutdown
- `47400ad` fix(auth): offline_access scope for refresh tokens

## Current Coverage
| Group | Cmds | Status |
|-------|------|--------|
| login/logout/account | 3 | Full (native OAuth2) |
| group | 2 | Full |
| vm (top-level) | 29 | Full |
| vm disk | 2 | Full |
| vm nic | 5 | Full |
| vm run-command | 6 | Full |
| disk | 8 | Full |
| vmss | 11 | Partial (no subgroups) |
| deployment | 44 | Full (group/sub/mg/tenant + ops) |
| network bastion | 9 | Full (custom WebSocket) |
| rest | 1 | Full |

## Architecture Constraints (do not violate)
- Manual reqwest for ALL OAuth2 (no `azure_identity` crate)
- No new dependencies without explicit approval
- `-g` short flag for `--resource-group` everywhere (matches az)
- Read-only testing only on live Azure — never test create/modify/delete
- `COMPUTE_API_VERSION = "2024-07-01"` (codebase-wide constant)
- Always update README when adding features
- Keep doc comments only for non-obvious invariants / complex algorithms / public API

## Test Environment
- Subscription: `62118f5c-be37-400f-9f20-a8b77a2a7877`
- Test VM: `hbv4_192958ff` in RG `EASTUS2-TST5-RG`, location `eastus2`
- Attached NIC: `nicconfig-104a6322`

## File Structure
```
src/
├── main.rs                         (~2312 lines, all CLI enums + dispatch)
├── arm_client.rs                   (~1235 lines, all ARM REST methods + LRO helpers)
└── commands/
    ├── disk/                       (8 cmds)
    ├── vm/
    │   ├── disk/                   (attach, detach)
    │   ├── nic/                    (list, show, add, remove, set + apply_primary helper in mod.rs)
    │   ├── run_command/            (invoke, list, show, create, update, delete + parse_params helper)
    │   └── [29 other vm commands]
    ├── vmss/                       (11 cmds)
    └── deployment/                 (44 cmds across 4 scopes)
docs/
├── authentication.md
├── bastion.md
├── deployments.md
├── managed-disks.md                (disk + vm disk)
├── resource-groups.md
├── rest.md
├── virtual-machine-scale-sets.md
└── virtual-machines.md             (vm + vm nic + vm run-command)
```

## Reusable Patterns
- **LRO polling**: `arm_client::poll_compute_lro` — handles 200/202/204, polls Azure-AsyncOperation then Location header. `.or_else(|_| Ok(...))` tolerates empty 200 bodies.
- **Dual-mode list/show**: see `vm/run_command/{list,show}.rs` — branches on `(rg, vm_name)` vs `(location)` args, errors on both/neither.
- **`k=v` param parsing**: `vm/run_command/mod.rs::parse_params(&[String]) -> Vec<Value>` splits into `[{name, value}]` JSON.
- **NIC primary invariant**: `vm/nic/mod.rs::apply_primary` enforces exactly-one-primary on PATCH.
- **Resource ID resolution**: bare names resolved in caller's RG, full ARM IDs passed through (see `vm/nic/mod.rs::resolve_nic_id`).

## Backlog (suggested next features)
- `vm extension` (list, show, set, delete, wait)
- `vm identity` (assign, remove, show)
- `vm boot-diagnostics` (enable, disable, get-boot-log, get-boot-log-uris)
- More vmss subgroups (extension, identity, run-command, nic)
- `network` beyond bastion (vnet, subnet, nsg, public-ip, nic standalone)
- `storage` (account, blob, container)
- `aks` (list, show, create, delete, get-credentials, scale, upgrade)
- `acr` (list, show, login, repository)
- **Tech debt**: tracing writes to stdout instead of stderr — pollutes JSON output. Fix in `src/main.rs` tracing init.

## Useful Session IDs (for resumption)
- Librarian — az vm run-command catalog research: `ses_2640c22d4ffePX2tyYqs6QR3tS`
- Librarian — `-g` flag research across az repo: `ses_2643fe196ffeLTOjl86F6GQJCX`
- Librarian — az vm nic research: `ses_2648bcabcffeHIf1FjYiIXV9jC`

## Known Quirks
- Librarian agent has dumped sprawling per-feature `.md` files into `docs/` before — always inspect output and consolidate into existing topic docs.
- Hook flags doc comments by priority — only keep for non-obvious invariants, complex algorithms, public API contracts. Trivial `/// Resolve NIC name to ID`-style comments get rejected.
